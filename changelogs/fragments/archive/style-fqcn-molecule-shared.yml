---

trivial:
  - >
    molecule test code - style only, no functional change: use FQCN for the
    remaining bare filter and test references in the shared playbooks
    (``ansible.builtin.to_nice_yaml``, ``ansible.builtin.to_nice_json``,
    ``is ansible.builtin.search``).
  - >
    ``mailserver_ldap_maildir`` playbook - style only, no functional change:
    use FQCN for the remaining bare filter references
    (``ansible.builtin.default``, ``ansible.builtin.string``,
    ``ansible.builtin.length``).
